- PGAdmin 💻 -

Para que você possa se conectar o banco é importante que você libere o acesso ao banco de dados.
    1. Liberar o acesso ao cluster em postgresql.conf
    2. Liberar acesso ao cluster para o suuario do banco de dados em pg_hba.conf
    3. Criar/editar usuarios.

Open PostgreSQL.conf
    # CONNECTIONS AND AUTHENTICATION
        listen_addresses = 'localhost'
        Observação : Não coloque o '*' porquê é considerado uma falha de segurança. Isso faz com que qualquer IP se conecte com o seu banco de dados.

        Todo comando em sql precisa terminar em ;

- Programa PGAdmin 💻 -
Iniciando o PostgreSQL no Windows:
    - Service > Procurar por PostgreSQL > Inicar
    https://www.ti-enxame.com/pt/postgresql/como-iniciar-o-postgresql-no-windows/824876259/

    - Seleciona o banco de dados > Aba Tools no Top > Query Tool
        No Query Tool se escreve os comandos do banco de dados.

        select 1;
        CREATE DATABASE aulaDB;
        depois vai até o DataBase e da Refresh para atualizar

    // Importante para Conexão:
        1. Liberar acesso ao cluster em postgresql.conf
        2. Liberar acesso ao cluster para o usuário do banco de dados em pg_hba.conf
        3. Criar/editar usuarios.